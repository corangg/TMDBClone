<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:id="@+id/item_movie"
    android:layout_marginEnd="@dimen/margin_end_layout"
    android:background="@color/black"
    android:orientation="vertical">
    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="@dimen/poster_radius">
        <ImageView
            android:layout_width="@dimen/item_now_playing_img_width"
            android:layout_height="@dimen/item_now_playing_img_height"
            android:src="@drawable/img_not_found"
            android:id="@+id/img_poster"/>
    </androidx.cardview.widget.CardView>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="@dimen/item_now_playing_text_height"
        android:background="@color/black"
        android:layout_marginTop="@dimen/item_now_playing_margin_top"
        android:textSize="@dimen/item_now_playing_text_title"
        android:textStyle="bold"
        android:ellipsize="end"
        android:id="@+id/text_title"
        android:maxLines="1"
        android:textColor="@color/white"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_smal"
        android:textColor="@color/logincolor"
        android:textSize="@dimen/item_now_playing_text_popularity"
        android:maxLines="1"
        android:id="@+id/text_popularity"/>
</LinearLayout>